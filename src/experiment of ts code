// Using 'type' to define the structure
type ProductType = {
  saler: string;
  consumor: string;
};

// Using 'interface' to define the structure
interface ProductInterface {
  saler: string;
  consumor: string;
}

// Initializing the object with 'type'
const productType: ProductType = {
  saler: "sham",
  consumor: "joit",
};

// Initializing the object with 'interface'
const productInterface: ProductInterface = {
  saler: "sham",
  consumor: "joit",
};

// Correctly defining a variable to store the product structure
const products: {
  saler: string;
  consumor: string;
} = {
  saler: "sham",
  consumor: "joit",
};

console.log(productType);
console.log(productInterface);
console.log(products);
